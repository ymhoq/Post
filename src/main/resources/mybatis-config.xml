<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Confi 3.0//EN" "http://www.mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<properties resource="db.properties"></properties>
	<typeAliases>
		<typeAlias type="com.solvd.post.mybatis.po.Car" alias="Car" />
		<typeAlias type="com.solvd.post.mybatis.po.Country" alias="Country" />
		<typeAlias type="com.solvd.post.mybatis.po.Payment" alias="Payment" />
		<package name="com.mybatis.po" />
	</typeAliases>
	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC" />
			<dataSource type="POOLED">
				<property name="driver" value="${jdbc.driver}" />
				<property name="url" value="${jdbc.url}" />
				<property name="username" value="${jdbc.username}" />
				<property name="password" value="${jdbc.password}" />
			</dataSource>
		</environment>
	</environments>
	<mappers>
		<mapper resource="sqlMapp/CarMapper.xml" />
		<mapper resource="sqlMapp/CountryMapper.xml" />
		<mapper resource="sqlMapp/PaymentMapper.xml" />
	</mappers>
</configuration>